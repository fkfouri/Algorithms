#
# Pseudo codigo de um automato
# nao implementado
#

def AFConstruct():
    for s in range(len(P)):
        for x in 'abc': #tamanho alfabeto
            


def AFMatch():
    s = 0                           #estado inicial 0

    for i in range(len(T)):         #todas as posicoes de T
        s = AF[s, T[i]]             #encontra o estado em que estou
        if (s == m)
            return i-m
    
    return -1
